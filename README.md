# Webapp

A minimal JS/HTML web application.

## Structure

- `index.html` - Entry point
- `styles.css` - Styles
- `app.js` - Application logic

## Run locally

Open `index.html` directly in a browser, or serve it:

```bash
npm start
```

This uses `npx serve` to serve the current directory at http://localhost:3000.
